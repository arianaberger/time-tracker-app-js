<% if flash[:notice] %>
  <font color="green"><%= flash[:notice] %></font>
<% end %>

<h1><%= @client.name %></h1>

<p>
<% if @projects.empty? %>
  Client does not have any assigned projects yet.
<% else %>
  <%= link_to "View Client Projects", client_projects_path(@client) %>
<% end %>
</p>

<%= link_to "+ Add a Project", new_client_project_path(@client) %>
<p><%= link_to "< View all clients", clients_path %></p>
