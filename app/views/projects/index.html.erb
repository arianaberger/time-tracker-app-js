<h1><%= @name.name %></h1>


<h3>Projects:</h3>

<ul>
<% @projects.each do |project| %>
  <li><%= link_to "#{project.name}", project_path(project) %></li>
  <% end %>
</ul>


<%= link_to "+ Add a new project", new_user_project_path(@name) %>