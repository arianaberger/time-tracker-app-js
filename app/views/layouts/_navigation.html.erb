<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand"><%= link_to "TimeTracker", root_path %></a>
    </div>

    <% if current_user %>
    <ul class="nav navbar-nav">
      <li class="active"> <%= link_to "Dashboard", dashboard_path %></li>
      <li><%= link_to "Your Time Entries", user_entries_path(current_user) %></li>
      <li><%= link_to "View Projects", user_projects_path(current_user) %></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li><span class="glyphicon glyphicon-user"></span> Logged in as: <%= current_user.name %></li>
      <li><span class="glyphicon glyphicon-log-in"></span> <%= link_to "Log Out", logout_path, method: :destroy %></li>
    </ul>

    <% else %>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "Login", login_path %></li>
      <li><%= link_to "Sign up", signup_path %></li>
    </ul>
    <% end %>

  </div>
</nav>